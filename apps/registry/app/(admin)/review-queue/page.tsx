/**
 * Admin review queue page (phase 1 placeholder).
 * Lists SUBMITTED plugin versions awaiting review.
 */
export default function ReviewQueuePage() {
  return (
    <div>
      <h1>Review Queue</h1>
      <p>Phase 1 placeholder — will list SUBMITTED plugin versions once the database is wired.</p>
    </div>
  );
}
