import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Registry is an independently deployed Next.js 15+ app. Keep configuration
  // minimal here; transpile / shared package settings can be added later when
  // shared workspace libraries are introduced.
};

export default nextConfig;
